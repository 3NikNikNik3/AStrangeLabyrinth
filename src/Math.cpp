#include "Math.hpp"

namespace AStrangeLabyrinth {
    namespace Math {
        // Vector
        Vector::Vector(float x, float y) : x(x), y(y) {}

        // Line
        Line::Line(Vector a, Vector b) : a(a), b(b) {}

    }
}

